setup:
    - echo something
    - echo this is an error >&2
env:
    TEST: test
    TESTTEST: testtest
exec:
    - echo 2
    - echo %TEST% # TODO: Printing *any* env. variable appends '"' (double quote) to it
    - echo %TESTTEST%